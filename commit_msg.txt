feat: allow re-linking WhatsApp + fix QR pairing and connection management

- Add POST /logout and POST /connect endpoints to bridge
- Add logout() and connect() methods to WhatsAppClient
- Move WhatsApp connection management to Settings dialog (Conexión tab)
- Remove connect/disconnect buttons from main toolbar
- Fix: bridge no longer auto-starts Baileys on launch
- Fix: logout now non-blocking (sock.end instead of await sock.logout)
- Fix: reconnect on restartRequired (515) to complete QR pairing
- Fix: pipe buffer deadlock (DEVNULL instead of PIPE)
- Fix: startBaileys guard against concurrent calls
- Fix: QR dialog auto-polls until QR is available
- Add browser config, qrTimeout, syncFullHistory:false
- Split settings into Mensajes and Conexión tabs
